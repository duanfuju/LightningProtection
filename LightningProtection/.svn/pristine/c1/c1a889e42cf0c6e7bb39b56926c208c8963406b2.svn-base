package cn.com.oking.dao;


import org.springframework.stereotype.Repository;


import cn.com.oking.entity.ProjectBasicInfo;

import com.bstek.dorado.hibernate.HibernateDao;

/**
 * 基本信息服务类
 * @author 寻香
 *
 */
@Repository
public class PbiDao extends HibernateDao<ProjectBasicInfo, Long>{
/*	public List<Object> findByTime(String date1,String date2){
		String sql ="select pbi.project_id,pbi.project_name,pbi.build_company,pbi.contact_person,pbi.contact_way,"
			     +"pbi.project_overview,pbi.contract_signing,pbi.report_status,pbi.contract_signing_person,"
			     +"pbi.contract_price,pbi.remark1,pbi.report_wite_person,pbi.check_personnel,pbi.completion,pbi.remark2 "
			     +" from project_basic_info pbi" 
			     +" where pbi.file_date between date'"+date1+"' and date'"+date2+"' ";
			
		Session session = super.getSession();
		SQLQuery query=session.createSQLQuery(sql);   
		List<Object> ls = query.list();
		return ls;
	}*/
	
}
